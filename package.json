{
  "name": "musteri-odeme-takip",
  "version": "1.0.0",
  "description": "Giyim mağazası için müşteri ödeme takip sistemi",
  "main": "server/index.js",
  "scripts": {
    "dev": "cd client && npm start",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "vercel-build": "cd client && npm install && CI=false npm run build",
    "start": "cd server && npm start",
    "install-deps": "npm install && cd client && npm install && cd ../server && npm install"
  },
  "keywords": ["müşteri", "ödeme", "takip", "taksit"],
  "author": "Marka World",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
} 